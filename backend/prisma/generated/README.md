# Legacy Prisma output directory

`prisma/generated/client` is a legacy generated output directory and is intentionally unused by the backend runtime.

The canonical Prisma client path for this repository is `src/generated/prisma`, configured in `prisma/schema.prisma`.
