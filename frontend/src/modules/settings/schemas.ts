import { z } from "zod";

/**
 * Placeholder schema for settings module.
 * Will be replaced once backend contract is defined.
 */

export const SettingsPlaceholderSchema = z.object({
  // TODO: define real fields based on backend response
}).passthrough();