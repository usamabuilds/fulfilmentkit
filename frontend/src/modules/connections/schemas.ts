import { z } from "zod";

/**
 * Placeholder schema for connections module.
 * Will be replaced once backend contract is wired.
 */

export const ConnectionsPlaceholderSchema = z.object({
  // TODO: define real fields based on backend response
}).passthrough();