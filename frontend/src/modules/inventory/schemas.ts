import { z } from "zod";

/**
 * Placeholder schema for inventory module.
 * We will replace this once backend contract is confirmed.
 */

export const InventoryPlaceholderSchema = z.object({
  // TODO: define real fields based on backend response
}).passthrough();