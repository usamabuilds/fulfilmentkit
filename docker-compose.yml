version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: fulfilmentkit_postgres
    environment:
      POSTGRES_USER: fulfilmentkit
      POSTGRES_PASSWORD: fulfilmentkit123
      POSTGRES_DB: fulfilmentkit
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: fulfilmentkit_redis
    ports:
      - '6379:6379'

volumes:
  postgres_data:
